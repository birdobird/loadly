version: '3.9'
services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: aiads
      POSTGRES_PASSWORD: aiads
      POSTGRES_DB: loadly
    volumes:
      - ./db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
