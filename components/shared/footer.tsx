export default function Footer() {
    return (
        <footer className="container py-8">
            <p>© {new Date().getFullYear()} Loadly. Wszelkie prawa zastrzeżone.</p>
        </footer>
    );
}